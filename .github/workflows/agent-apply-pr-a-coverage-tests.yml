name: Agent Apply PR A Coverage Tests

on:
  push:
    branches:
      - agent/core-issue-message-contract

permissions:
  contents: write

jobs:
  Apply:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: agent/core-issue-message-contract

      - name: Apply coverage tests
        shell: bash
        run: |
          set -euo pipefail
          base64 --decode .github/agent/pr-a-coverage-tests.patch.gz.b64 | gzip --decompress > /tmp/pr-a-coverage-tests.patch
          git apply --check /tmp/pr-a-coverage-tests.patch
          git apply /tmp/pr-a-coverage-tests.patch
          rm .github/agent/pr-a-coverage-tests.patch.gz.b64
          rm .github/workflows/agent-apply-pr-a-coverage-tests.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add --all
          git commit -m "test: cover static message fast paths"
          git push origin HEAD:agent/core-issue-message-contract
