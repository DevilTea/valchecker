name: Agent Apply PR A Review Fixes

on:
  push:
    branches:
      - agent/core-issue-message-contract

permissions:
  contents: write

jobs:
  Apply:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: agent/core-issue-message-contract

      - name: Apply reviewed fixes
        shell: bash
        run: |
          set -euo pipefail
          base64 --decode .github/agent/pr-a-review-fixes.patch.gz.b64 | gzip --decompress > /tmp/pr-a-review-fixes.patch
          git apply --check /tmp/pr-a-review-fixes.patch
          git apply /tmp/pr-a-review-fixes.patch
          rm .github/agent/pr-a-review-fixes.patch.gz.b64
          rm .github/workflows/agent-apply-pr-a-review-fixes.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add --all
          git commit -m "fix: preserve external issue message scopes"
          git push origin HEAD:agent/core-issue-message-contract
