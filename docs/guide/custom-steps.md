# Custom Steps

This legacy route is retained temporarily for compatibility while the final route cleanup is handled by #154.

Use:

- [Custom Step Plugins](/extending/custom-step-plugins) to decide when a reusable plugin is appropriate and build a checked first plugin;
- [Plugin Composition and Distribution](/extending/plugin-composition-and-distribution) for capabilities, metadata, registration order, selective instances, interoperability symbols, and tree-shaking boundaries;
- [Valchecker 1.0 Contract](/guide/v1-contract) for the formal supported extension surface.

The former built-in contribution checklist is repository-maintainer workflow and remains owned by `AGENTS.md` and the `valchecker-dev` skill rather than being duplicated in public consumer documentation.
