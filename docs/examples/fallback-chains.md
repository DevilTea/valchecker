# Fallback Chains

This legacy route is retained temporarily for compatibility while the final route cleanup is handled by #154.

Use:

- [Fallback and Recovery](/guides-recipes/fallback-and-recovery) for recovery-boundary design, defaults, multiple boundaries, and async recovery;
- [Synchronous and Asynchronous Execution](/core-concepts/sync-and-async) for path-dependent async completion;
- [`fallback()` Reference](/api/helpers#fallback) for exact recoverable categories, callback behavior, and owned issues.

The canonical guide reuses the checked JSON-recovery fixture rather than maintaining a second copy here.
